# hst

```
Usage: hst [flags]

Flags:
  -h, --help              Show context-sensitive help.
  -t, --status-type=""    HTTP status code types name to filter
  -c, --code=""           HTTP status code to filter i.e:(401, 222, 1xx)
```
